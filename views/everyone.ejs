<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/styles.css">
    <title>Everyone</title>
</head>
<body>
    <div class="container">
        <div class="content">
            <h1>Birthdays of Everyone Currently Existing</h1>
            <% if (everyone.length > 0) { %>
                <ul>
                    <% everyone.forEach((person) => { %>
                        <li>Name: <%= person.name %> - Birthday: <%= person.bday %></li>
                    <% }) %>
                </ul>
            <% } else { %>
                <p>No birthdays found in the database.</p>
            <% } %>
            <a href="/"><button class="back-btn">Back to Home</button></a>
        </div>
    </div>
</body>
</html>

